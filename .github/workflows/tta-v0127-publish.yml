name: Torn Trade Analyzer v0.1.27 compact launcher and periods (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'One-time v0.1.27 publisher is disabled after successful release.'
