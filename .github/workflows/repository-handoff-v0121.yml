name: Completed repository handoff record

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'v0.1.21 repository handoff completed; workflow intentionally disabled.'
