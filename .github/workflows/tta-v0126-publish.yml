name: Torn Trade Analyzer v0.1.26 abroad verification (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'v0.1.26 release workflow disabled after successful publish.'
