name: Completed migration record

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Torn Trade Analyzer migration completed; workflow intentionally disabled.'
