name: Torn Trade Analyzer v0.1.25 continuous TCT timeline (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'v0.1.25 was published and verified; this one-time workflow is disabled.'
