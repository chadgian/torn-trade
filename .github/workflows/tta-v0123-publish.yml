name: Archived Torn Trade Analyzer v0.1.23 release

on:
  workflow_dispatch:

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "v0.1.23 release workflow archived"
